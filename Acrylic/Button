local Button = {}
Button.__index = Button

function Button.new(text)
    local button = Instance.new("TextButton")
    button.Text = text
    button.AutoButtonColor = false
    return setmetatable({Instance = button}, Button)
end

return Button
