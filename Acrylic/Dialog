local Dialog = {}
Dialog.__index = Dialog

function Dialog.new(title, description)
    local dialog = Instance.new("Frame")
    -- Dialog implementation
    return setmetatable({Instance = dialog}, Dialog)
end

return Dialog
