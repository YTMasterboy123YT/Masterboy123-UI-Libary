local Fluent = loadstring(game:HttpGet("https://github.com/dawid-scripts/Fluent/releases/latest/download/main.lua"))()
local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer

local Window = Fluent:CreateWindow({
    Title = "Head Control",
    TabWidth = 160,
    Size = UDim2.fromOffset(450, 360),
    Theme = "Dark"
})

local MainTab = Window:AddTab({ Title = "Main" })

MainTab:AddToggle({
    Title = "Teleport All Heads",
    Default = false,
    Callback = function(value)
        _G.HeadTeleport = value
        
        while _G.HeadTeleport do
            for _, player in ipairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character and player.Character:FindFirstChild("Head") then
                    player.Character.Head.CFrame = LocalPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, -3)
                end
            end
            task.wait(0.1)
        end
    end
})
